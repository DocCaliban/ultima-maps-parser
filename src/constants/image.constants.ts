/**
 * Number of bits per pixel
 */
export const BITS_PER_PIXEL = 4;

/**
 * Total bits in a byte
 */
export const BITS_PER_BYTE = 8;

/**
 * Pixels encoded per byte
 */
export const PIXELS_PER_BYTE = BITS_PER_BYTE / BITS_PER_PIXEL;